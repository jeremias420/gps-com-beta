package main

import (
	"gps-com/app"
	// srv "github.com/soflex/go-package-services"
)

func main() {
	/*isService := false
	name := "gps-com-bulk"
	description := "gps-com-bulk"
	version := "v1.0.0"

	 srv.ManageService(isService, name, description, version, app.Start)
	*/
	app.Start()
}
